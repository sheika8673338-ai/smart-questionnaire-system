// Local development: keep empty, so requests use the same origin.
// Production: set your CloudBase HTTP function origin, without a trailing slash.
// window.SMART_SURVEY_API_BASE = "https://example.service.tcloudbase.com";
window.SMART_SURVEY_API_BASE = "";

// Keep this empty in public deployments.
// When opening /?admin=1, the page will ask you to enter the admin token.
window.SMART_SURVEY_ADMIN_TOKEN = "";
